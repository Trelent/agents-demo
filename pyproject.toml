[project]
name = "demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Calum Bird", email = "calum@trelent.net" }
]
requires-python = ">=3.11"
dependencies = [
    "trelent-agents>=0.1.2",
]
